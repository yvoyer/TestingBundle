imports:
    - { resource: security.yml }

framework:
    secret: "notsosecretsecret"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    templating:
        engines: ['twig']
